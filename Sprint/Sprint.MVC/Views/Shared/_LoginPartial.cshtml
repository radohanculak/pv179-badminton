@using Microsoft.AspNetCore.Identity
@using Sprint.BL.Dto.User
@inject SignInManager<UserDto> SignInManager
@inject UserManager<UserDto> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <span class="nav-link text-dark">Hello @User.Identity?.Name!</span>
        </li>
        <li class="nav-item">
            @using (Html.BeginForm("Logout", "Identity", new { returnUrl = Url.Action("Index", "Home") }, FormMethod.Post))
            {
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            }
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" href="@Url.Action("Register", "Identity")">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" href="@Url.Action("Login", "Identity")">Login</a>
        </li>
    }
</ul>